# Findings

## 2026-08-02 S06 pre-commit review

- Recovery Git audit confirms the staged diff is empty, git diff check passes, and the protected manifest diff is the same separately owned baseline; it must stay excluded from the phase commit.
- Shared ownership core review is structurally sound: one local SharedTextureImage alias, backend-compatible const references rebound by explicit table constructors/assignments, moved-from handles retained to prevent dangling references, and transactional publication after sidecar decode.
- Authoring review confirms no-Override shares the Source Specular handle, actual Overrides edit a local mutable candidate and publish only after claims/conflicts complete, and shared material construction counts unique image identities before the final table swap.
- Strict import review confirms exactly one immutable handle is published per decoded channel, Import Result and material reuse those handles, metadata-only Session validation is separated from full snapshot validity, and the temporary cache copy path is shallow pending S07.
- Export/render/UI review is pointer-only: overwrite discovery, staging, rollback, exact Iris-byte verification, nearest/LOD behavior, and UI layout remain unchanged.
- AppSession review preserves local-candidate commits and success-only cache clearing. One budget-audit question remains: buildSessionLabPbrMaterial adds unique candidate handles as resident image counts while retained_resident_bytes may already include the same current handles; inspect estimator semantics and remove any double count if real.
- Estimator inspection confirms the overlap is real: resident_fixed_bytes is added to resident_rgba_image_count times atlas bytes, and every one of the ten product calls currently passes a current-Session byte total that already owns most/all candidate handles.
- Audit correction applied: each product call now passes its fixed-capacity resident identity counter, buildSessionLabPbrMaterial adds candidate handles into that same counter, and the estimator receives the union only as resident_fixed_bytes. All ten call sites are migrated and git diff check remains clean.
- Regression diff review is complete: AppSession asserts metadata-only source state, exact Base/Normal/Specular identity, no-Override sharing, first-Override COW, release on clear, and unchanged uncached reload semantics; viewport tests assert 2K three-handle identity, import/cache shallow copies, Iris sharing, export byte equality, and source contracts. No assertion was removed or weakened.
- S06 final scope is clean and green: three affected Release targets build, related CTest passes 2/2 in 1.48 seconds, actual 2K stress proves exactly three shared decoded images, git diff check passes, and all forbidden subsystems plus the protected baseline remain untouched.

## 2026-08-02 authorized resume

- Recovery projection error after renewed authorization: reading `findings.md` in two 140-line windows still exceeded the combined tool-output cap and was truncated. This was read-only; recovery will use four smaller exact windows before any S05 code edit.
- Recovery state is consistent with the persisted S04 checkpoint: branch `xpbdRT`, `HEAD` `a9b955d`, four task commits ahead of the starting point, S04 code/planning changes uncommitted, and the separately recorded protected user baseline untouched.
- The newest affected test evidence is viewport/AppSession CTest 2/2 PASS in 1.53 s. It covers the actual 2K compact three-image allocation, 8K checked arithmetic rejection without image allocation, and exhaustive compact-vs-reference sampling for the small synthetic fixture.
- Recovery command error: `Join-Path $planRoot 'task_plan.md', ...` was parsed as an object-array `ChildPath` and did not read the planning files. This was read-only and caused no repository change; use a newline-delimited explicit string array for subsequent multi-file reads.
- Recovery projection note: a 240-line/59,188-byte full findings read was truncated by the tool-output cap even when requested in two sections. Continue with smaller exact line windows so no persisted finding is skipped; this is a read-only tooling limitation.
- Recovery diff audit confirms one unchecked multiplication remains in `sessionLabPbrResidentBytes` (`texels.capacity() * sizeof(uint32_t)`) and the current final-model preflight runs after file decoding for Base/direct Specular/Iris/strict Suite. Both are S04 correctness gaps, not failed verification repairs; address them before the final S04 affected build/tests.
- The original plan fixes S04 commit 5 as `perf: remove persistent resolved material texel expansion`. Its exact gate remains zero resolved stride plus all small-image texels matching the unchanged decoder; budget rejection must occur before allocation and include current residency, encoded snapshots/decoder temp, candidate images, Coverage, Composition, and Cache.
- A combined original-plan/API inventory exceeded the output projection after returning the required plan section and key call sites. No state changed; use exact bounded source ranges for texture header inspection, strict Suite snapshot/decode order, and Iris import before implementation.
- `TextureDecodeLimits` already models retained state, encoded input, decoder buffer, and decoded candidate; `loadTextureImage` can reject an encoded snapshot before allocation by subtracting retained bytes from the effective peak ceiling. A new transaction-preserving header-only inspection API can reuse stb's memory info path and checked dimension/peak arithmetic without allocating pixels.
- Strict Suite currently snapshots all sources and decodes Base/Specular/Normal before cache lookup or material building. Its S04 preflight must account for cumulative encoded snapshots, current Session residency, current cache, decoder scratch, decoded locals, compact material copies, Coverage, Composition, and the current deep-copy cache behavior that will only be removed in S07.
- Iris currently holds a decoded candidate and then copies the immutable snapshot into a separate byte vector. Until S06 removes that duplication, S04 must check the snapshot allocation, decoder peak, and the later `snapshot + copied original + decoded` peak separately before each allocation.
- Pre-compile AppSession audit caught an access-boundary issue: anonymous-namespace limit helpers cannot read the private `labpbr_import_cache_` member. No build was attempted and no repair round is consumed; pass the already available `residentBytes()` value from each member call site into the helper.
- The final S04 affected Release build compiles all three required targets after the budget changes without a repair round. The App target also proves the public/default API changes did not leave an uncompiled production-only call site.
- Final S04 related CTest is green 2/2 in 1.35 seconds. Structural search finds no `ResolvedMaterialTable::texels` member/indexing in production/tests, the compact stride constant is zero, `git diff --check` is clean, and no large generated fixture entered the task diff.
- S04 final scope is clean: no Vulkan/backend/shader/physics/UI path is modified; Release App plus both affected regression targets are green; the protected tracked manifest and historical untracked evidence remain outside staging. The compact 2K peak is 234,881,024 bytes versus the legacy 687,865,856-byte baseline model, while 8K is rejected by arithmetic without three-image allocation.

## S05 lazy Coverage / Composition facts

- S05 final gate is green after one compile-only repair. No-Override Session state owns no Coverage runs or Composition pixels; actual 2K full coverage compresses to 2,048 row runs; 8K run arithmetic rejects before allocation; source/default export materializes only after overwrite approval and preserves transactional state.
- AppSession diff review confirms Coverage reuse is safe at every enabled call: the sole topology-changing model path supplies no reuse context; all other paths rebuild a mesh and require exact Domain/atlas equality before copying compact runs. First Override therefore builds, subsequent material-only edits reuse, and clearing the last Override drops Coverage.
- Final affected tests exercise the actual 2K atlas and produce exactly 2,048 runs for 4,194,304 covered texels (one inclusive run per row), while source contracts confirm no production texel Set nodes. Both deferred Source and default exports leave resident Composition empty and round-trip exact expected bytes.
- Structural inventory after migration finds texel `std::set<uint32_t>` only in the regression's reference-expansion helper; production Coverage has no flat texel container or Set/Unordered Set node. Add a source-contract assertion for `marked`, `touched`, `sort`, and absence of the forbidden texel set spelling.
- The product large-UV regression currently expects resident Coverage immediately after a suite import with no Overrides; S05 must replace this with the stronger lazy invariant, then apply one real group Override to prove Coverage runs appear for both eyes before testing rollback.
- `LabPbrSuiteImportLimits` is the predecode path that currently reserves one full Composition and flat Coverage unconditionally. Add an override-presence flag (source-compatible default false) and feed it from AppSession so strict imports receive the same lazy run peak as the later product helper.
- The generic S04 estimator remains a structural arithmetic primitive and its historical baseline assertions need not be rewritten. S05 product preflight should supply zero lazy bytes with no Overrides, or a conservative `marked + touched + worst-case runs` Coverage peak plus one Composition image with Overrides; strict-suite predecode limits must receive the same override state to avoid false no-Override rejection.
- Existing viewport tests already provide a 2x2 overlap reference, conflict oracle, transactional Coverage failure, and large-UV membership checks. They can expand runs only inside tests for exact-set equality and add one runtime 2K full-quad stress without committed assets. AppSession's transaction snapshot must compare `group_runs` plus deferred materialization metadata.
- AppSession export always refreshes authoring before the initial request, then confirmation uses the current committed composition. Passing current Base dimensions and current Source Specular to both export calls preserves this behavior while enabling deferred materialization; no new pending-session ownership is needed.
- Draft initialization already has a safe default byte tuple. Replacing `texels->front()` with `firstTexel()` means no-Override/empty-Coverage sessions simply keep those defaults, while existing Overrides still have run Coverage and initialize from the same source texel.
- Export checks destination and overwrite before creating staging directories, but currently validates Iris dimensions against resident Composition first. A deferred export descriptor must supply atlas dimensions/source Specular; overwrite detection remains allocation-free, then a local full image is materialized and used for normal validation, PNG encoding, and round-trip checks.
- `buildSessionLabPbrMaterial` currently budgets both a linear Coverage vector and a full Composition for every valid Base, then always builds both. S05 must make `coverage_peak_bytes` and Composition candidate bytes conditional on nonempty Overrides; the resolved material already selects Source Specular directly when Overrides are empty.
- Exact authoring audit confirms S05 can short-circuit empty Overrides before requiring a valid Coverage object, while still validating atlas/source dimensions. The user-specified persistent ABI is row-aware inclusive `UvRun{y,x0,x1}`; this supersedes the original compact-plan sketch using linear `first/count` and avoids runs crossing rows.
- Renewed symbol inventory confirms every remaining flat-Coverage consumer is confined to authoring, AppSession accounting/draft/snapshots, one panel count, and regressions; no shader/Vulkan/physics consumer requires an ABI change. Export and tests are the only additional deferred-composition surfaces.
- Coverage currently stores sorted flat texel indices in `std::map<std::string, std::vector<uint32_t>>`, but construction first inserts every texel into one `std::set` per group. Composition always allocates and initializes `width*height*4` bytes before it checks whether any override exists.
- The persistent Coverage API has only three production consumers: composition iteration, AppSession draft's deterministic first texel, and the LabPBR panel's displayed covered-texel count. Run iteration, `firstTexel`, and `texelCount` accessors cover all three without expanding persistent texel vectors.
- AppSession export currently requires resident `labpbr_composition.specular`. To keep no-Override state nonresident, export must materialize a local candidate transactionally: copy Source Specular when present, otherwise create the default `_s` image only at the actual export request/confirmation boundary.
- `buildSessionLabPbrMaterial` is the lazy commit point: with no overrides it can retain only Domain/resolved material plus deferred-export metadata, and clear Coverage/Composition pixels; with overrides it builds or reuses run Coverage and creates Composition. Existing transaction locals preserve rollback.
- `loadSelectedLabPbrDraft` uses Coverage only for a deterministic first texel. The run structure supplies that directly when overrides exist; with no overrides, the draft safely retains its existing standard defaults instead of forcing full Coverage generation, satisfying the explicit no-Override generation gate.
- The one UI coverage label can call `texelCount(group)`; this is a data-access substitution only and does not change layout. Transaction snapshots must compare run maps and deferred-composition metadata in addition to existing texture/conflict/error state.
- The existing localized Coverage label uses `%zu`; the run count accessor remains 64-bit internally and the panel will saturate-cast to `size_t` solely at this display boundary, preserving the format and layout.
- Export needs to delay default/source materialization until after overwrite detection. Extending the export call with deferred dimensions/source lets the existing transactional staging function perform local materialization only once an actual write proceeds, while direct callers with resident Composition remain source-compatible.

## S06-S07 expected structural benefit

- S06 removes duplicate decoded-image and Iris encoded-byte ownership across Session/Source/Resolved/Import state. Immutable assets become shared, while authored Specular uses copy-on-write only when the first Override is applied and releases that copy after Overrides are cleared.
- S07 converts the current unbounded by-value import cache into a shared-asset, byte-accounted 256 MiB LRU. Cache hits stop copying full images, cold entries are evicted predictably, and an individually oversized asset bypasses caching without failing the import.
- Together with S05, Gate B can prove that no-Override sessions avoid full Coverage/duplicate Composition, shared pixels and Iris bytes are counted once, and cache residency remains bounded under 2K/4K stress while transaction rollback remains intact.
- S06 recovery is complete at green `HEAD=58445da`: the S05 commit owns exactly its 14 audited paths, the newest related tests are 2/2 PASS, and no task code is dirty. The only non-planning worktree changes remain the protected user baseline.
- S06 ownership inventory confirms the duplicate chain is local and explicit: `AppSession::model_texture`, `ImportedLabPbrSuite::base_image`, `ResolvedMaterialTable::{base,normal,specular}_image`, `LabPbrCompositionResult::specular`, and `ReadOnlyIrisNormalAsset::decoded` are independent `TextureImage` values. Iris encoded bytes are also copied from the suite snapshot into a separate vector.
- Vulkan directly reads `ResolvedMaterialTable::normal_image/specular_image` fields. S06 therefore needs a compatibility-preserving shared-image handle or accessor strategy that leaves `vulkan_backend.cpp` untouched, rather than changing backend resource ownership or upload behavior.
- The plan's intended ownership type is an alias, `SharedTextureImage = std::shared_ptr<const TextureImage>`, and explicitly excludes a project-wide `TextureImage` storage rewrite. App/UI consumers may move to pointer access, but `ResolvedMaterialTable` needs a read-only compatibility surface for the unchanged Vulkan source while its real owners become shared pointers.
- `ImportedLabPbrSuite`, `LabPbrCompositionResult`, and `ReadOnlyIrisNormalAsset` have bounded LabPBR-local APIs and can directly adopt shared handles. Material construction/import signatures can accept or return handles without changing decoding, filtering, or channel math.
- `ResolvedMaterialTable` currently relies on implicit default copy/move assignment and public mutable image fields; only construction/tests mutate those fields, while Vulkan is read-only. A custom table implementation can own shared handles and expose stable read-only image references, provided copy/move constructors and assignments explicitly rebind those references and all construction moves to one setter/factory path.
- Strict import currently decodes Base/Specular/Normal into locals, then `buildAuthoredResolvedMaterial` deep-copies all of them, and finally moves Base separately into `ImportedLabPbrSuite`. Its current cache then makes another by-value suite copy. S06 can share one decoded handle per channel immediately after decode; S07 will separately change cache policy and lookup timing.
- `AppSession` is singleton-owned and already has an explicit constructor, so its public Base field can directly become `SharedTextureImage`; all product/UI/render consumers can dereference or pass `.get()` without introducing class copy semantics. This is separate from the material table's Vulkan-compatible read-only references.
- Session resident accounting currently counts the same Base/Normal/Specular pixel vectors repeatedly across Session, source material, resolved material, Composition, and Iris. S06 must count shared asset identities once and likewise count shared encoded-byte identities once; the material preflight's candidate image count can drop to only the actual authored Specular copy when Overrides exist.
- Long-lived `labpbr_suite_source` change detection uses only path/presence/size/time/hash, not encoded contents. Imported/cache results still need snapshot validity, but Session can retain a metadata-only copy with all `original_bytes` cleared; Suite Normal bytes instead move directly into Iris's shared encoded handle.
- Composition mutation is already isolated in one local result. S06 should materialize and edit a local mutable `TextureImage`, then publish one `shared_ptr<const TextureImage>` only after every claim/conflict pass completes; no-Override remains a null handle. This is the required copy-on-write transaction boundary.
- Iris import currently budgets and performs a second encoded vector copy after snapshot decode. Replacing both Iris fields with shared handles eliminates that copy and its special peak term: `original_file_bytes = snapshot.bytes`, `decoded = make_shared<const TextureImage>(move(decoded))`, with output assignment last.
- Strict import's preflight currently models 2x decoded material images, an extra Base, optional Iris decoded/encoded copies, and possible cache deep copies. Under S06 the live decoded Base/Normal/Specular handles are shared by result/material/Iris/cache, so the structural candidate count becomes the decoded channel count plus one authored Composition image only when Overrides exist; cache adds no image copies even before S07 policy changes.
- Material-image mutation sites are narrowly bounded: two construction implementations, four AppSession sidecar keep/add/remove operations, and one small direct-mutation regression fixture. All can move to `setImageAssets`/shared-handle construction; every other material image access is read-only.
- A `ResolvedMaterialTable` with private shared owners plus public const image references preserves the exact backend read syntax. Custom copy/move constructors and reconstructing assignments must bind each reference to its new owner's target (or one static empty image); move construction deliberately copies the shared handles so a moved-from table's references cannot dangle.
- Re-reading the original S06 text confirms the exact invariants: shared Session/Source/Resolved/Import/Iris decoded assets; no-Override Composition shares Source Specular, first Override creates the authored copy, failed candidates are discarded, and clearing Overrides releases it. Session source state must be metadata-only.
- Existing regressions directly mutate only one handcrafted material Base and one export Composition. Those fixtures can construct immutable handles explicitly; new identity assertions should compare `.get()` pointers across Import Result, Source/Resolved, Session, Composition, and Iris, plus verify failed/cleared Overrides do not retain authored handles.
- Legacy best-effort material resolution loads sidecars into mutable locals before publishing the table, so it can also create all shared handles once at the end. No decoder or sidecar validation needs to write through a material reference.
- Export can consume immutable handles without changing staging: select `composition.specular.get()`, dereference Iris decoded/encoded handles for validation/write, and compare round-trip byte contents rather than shared-pointer identity. Overwrite discovery still occurs before any deferred default materialization.
- Suite commit is the exact metadata-strip boundary: copy the validated source descriptors, clear each `original_bytes`, and commit that metadata-only value only after Domain/Composition/material candidates succeed. Before stripping, Iris takes the exact Normal snapshot pointer and material Normal image pointer.
- Product cache is intentionally disabled during S06 by importing with `cache=nullptr`; any pre-existing cache remains counted and untouched on failure, then is cleared only at a successful suite commit. S07 will install and re-enable the bounded shared LRU. This satisfies the required no-old-unbounded-cache transition.
- Direct Base import should publish its decoded local once into a shared handle, retain compatible Source Specular/Iris handles by identity, and pass those handles through both source and resolved material candidates. The current candidate transaction makes this conversion local until the final Session assignment.
- Direct Specular import similarly publishes one immutable handle before copying the Source table, while remove operations replace only the relevant shared handle. Iris removal must clear the Source Normal handle as well as the separate read-only Iris owner so the last reference is actually released when no other cache/result owns it.
- Production ownership search is clean after AppSession wiring: no published material image is assigned or cleared through a const image reference, and no value-style Composition/Iris/Base field access remains. Remaining old syntax is confined to regression fixtures that must be migrated next.
- Existing AppSession tests assumed a full snapshot-valid Session source and an empty deferred Composition even when Source Specular exists. S06 intentionally strengthens these: Session source is metadata-valid with all byte pointers null, and no-Override Composition holds the exact same Specular handle as Resolved/Source without duplicating pixels.
- The former Iris “encoded-byte copy” rejection threshold is now a positive sharing gate: the same tight peak that rejected the extra copy must succeed because only the snapshot, decoder, and immutable decoded candidate coexist. Subsequent invalid replacement must preserve the newly published handles exactly.
- Export tests must replace in-place Composition mutation with publication of a fresh immutable Specular candidate. Deferred export still accepts a raw Source view derived from that handle and must leave the deferred handle null.
- The still-unbounded cache class is exercised only as a lower-level S06 transition check; copied/cached suites must preserve Base/Normal/Specular handle identity, proving the former by-value structure no longer deep-copies images. Product use remains disabled until S07 adds its byte-bounded LRU.
- Because Session deliberately drops Suite snapshot handles, exact Iris-to-Suite encoded pointer identity is no longer externally observable after commit. Add a source contract for direct pointer assignment plus runtime exact-byte/metadata-null assertions; decoded identity remains directly observable and is already asserted.
- `resolveLabPbrMaterial` is used only by regressions, while product/strict paths use `buildAuthoredResolvedMaterial`. Keep a value-reference compatibility overload for small callers, add a shared-handle overload for the real chain, and migrate the existing 2K stress to handles so it allocates exactly three images rather than creating hidden copies.
- The material compatibility core is implemented without touching Vulkan. Table assignments reconstruct from a fully prepared replacement so its const references always bind to the matching shared owners; copy/move construction retains handle ownership for moved-from reference safety.

## Repository facts

- Initial repository: `D:/XPBD_Baker_CPP`; branch `xpbdRT`; initial HEAD `3bd2109d6dfaf2ac257b27a7fbe26b0d2a53ebc0`; fetched upstream HEAD is `0a571c9cf195e88eea3c9df62eb7e2f046c52c44` (three commits ahead).
- The supplied plan was read completely from `C:/Users/shiromizu/Desktop/xpbdRT_大UV错位与LabPBR内存优化_无人值守精简版.md` (13,310 bytes).
- Session catch-up produced no unsynchronized planning report.
- The project is C++23 and primarily Windows/MSVC. The documented full gate uses configure preset `s00-gate`, Release build preset `s00-gate-release`, and test preset `s00-gate-release`.
- Existing regression executables are `xpbd_viewport_regression_tests`, `xpbd_nuklear_focus_regression_tests`, and `xpbd_app_session_regression_tests`; the Release gate also builds the app, CLI, and `xpbd_check_spirv`.
- Both root READMEs, every CMake/preset/vcpkg file, and all three existing test sources (5,753 lines total) have now been read completely.
- The preset pins `VCPKG_ROOT=C:/vcpkg`, shared dependencies under repository `vcpkg_installed`, and the Visual Studio 2022 x64 generator. Streamline is optional and never downloaded implicitly.
- `xpbd_viewport_regression_tests` already links the viewport, texture, LabPBR import/material/authoring/export, RT records, ray tracing, world, and preview sources. `xpbd_app_session_regression_tests` is the product-entry transaction test target. New S00/S01 coverage can therefore live in the existing test binary without creating heavyweight application launches.
- Relevant production file sizes and the complete loader/data model were inspected before baseline changes.

## Protected pre-existing working-tree baseline

- Tracked user change: `docs/long_term_scene_plan_manifest.json`.
- Untracked user/history evidence: `artifacts/**`, `docs/perf/**`, `docs/pre_cleanup_DC00_baseline.md`, `docs/pre_cleanup_handoff.md`, `docs/pre_cleanup_manifest.json`, `docs/pre_performance_handoff.md`, and `docs/pre_performance_manifest.json`.
- These paths are outside the planned implementation surface. They must not be edited, deleted, reset, cleaned, or staged. Phase-clean means the Git status equals this protected baseline after the phase commit.

## Execution decisions

- Stage files by explicit path only. Never use broad `git add` in this pre-dirty worktree.
- The fetched upstream range changes only four historical `docs` files and deletes `docs/long_term_scene_plan_manifest.json`, which has a protected local modification. Production source/CMake/test content has no net difference. Do not fast-forward, stash, reset, or merge this range; continue from the checked-out local HEAD so the user change is untouched.
- Root-level legacy planning files and `.planning/` exist but are outside this task. The only active task memory is `docs/plans/large_uv_labpbr/`.
- Synthetic PNG/model resources are generated in test temporary directories; no 2K/4K assets are committed.
- Relevant production surfaces are `model_loader`, `viewport_mesh`, `texture_image`, `labpbr_{material,import,authoring,export}`, AppSession material transactions/dialogs, RT material records/shaders, and the model-atlas sampler creation in `vulkan_backend.cpp` only.
- `LabPbrSourceFile::original_bytes` already uses `shared_ptr<const vector<uint8_t>>`, so S06 must extend and reduce ownership from the actual current model rather than assume every compressed snapshot is a direct vector.
- Current Win32 import dialogs in AppSession use `GetOpenFileNameW` with fixed `MAX_PATH`; `IFileOpenDialog` is absent. The static model-atlas Vulkan sampler is nearest + Repeat, while other Repeat samplers serve unrelated resources and must remain untouched.

## Existing test coverage discovered

- `src/gfx/viewport_regression_tests_main.cpp` (3,933 lines) has been read completely.
- `src/app/app_session_regression_tests_main.cpp` (1,310 lines) has been read completely. External suite execution is optional via a positional path; the normal CTest path is fully synthetic and temporary-directory based.
- `src/app/nuklear_focus_regression_tests_main.cpp` (510 lines) has been read completely. It is outside this task's implementation scope and confirms the UI test drives the real product helper across English/Chinese and DPI scales; no UI layout changes are needed.
- `viewport_regression_tests_main.cpp` already has source-contract assertions for explicit LOD 0 and nearest filtering across Raster/RT/PT material samplers, plus a 28-binding RT descriptor ABI contract. S02 must extend these contracts for model-atlas clamp without weakening current assertions.
- Texture decode tests already verify checked RGBA arithmetic, 16,384 side/134,217,728-pixel limits, pre-decode header rejection, decoded/peak budgets, overflow rejection, and preservation of the caller's output object on failure.
- Existing LabPBR discovery tests directly assert `ResolvedMaterialTable::texels`, confirming the persistent decoded table is current behavior and that S04 must replace those expectations with on-demand reference checks.
- Strict suite import currently retains exact Base/Normal/Specular/properties compressed snapshots and caches successive checksum versions as separate entries. Tests cover missing/corrupt/mismatched sidecars, format confirmation, optional normals, source-change detection, and failure without cache damage; S06/S07 must deliberately update ownership and cache assertions.
- Coverage currently stores per-group flat texel-index vectors (`group_texels`), rasterizes from float mesh UVs, deduplicates overlaps, and Composition immediately owns a full `TextureImage`. These tests provide the reference set/conflict oracle for S05 run encoding.
- Iris normal currently owns `original_file_bytes` directly and failure-preservation is already asserted. Export round-trips those exact bytes; S06 must preserve that behavior while changing ownership to shared immutable storage.
- RT tests already model material generation as one of six independent scene-generation inputs and assert history-reset classification. S02 should reuse that generation path; it must not alter RR/SR/FG protocols or descriptor topology.
- Existing canonical cube tests cover Box UV mirror/tangent handedness, Per-Face north rotation, and the Bedrock Up-face opposite-corner rule. They currently assert normalized float UVs divided by one 16×16 declaration, so S01/S02 should add a double raw-UV/domain layer while preserving these GPU-facing results.
- Static alpha classification currently samples the texture from normalized mesh floats and assigns Opaque/Cutout/Blend before draw-plan ordering. This is a concrete S02 consumer that must take the resolved domain and clamp semantics transactionally.
- Hardware eligibility has deterministic CPU tests for NVIDIA vendor/features/generation. The suite can report unsupported hardware without treating it as a correctness failure; shader/source contracts remain the non-hardware Gate A evidence.
- AppSession tests define a full material transaction snapshot: model texture, resolved material, coverage, overrides/draft, composition, Iris normal, suite source, texture path, material generation, source-change flag, and cache-hit flag. New failure tests should reuse/extend this equality oracle so no partial state replacement is missed.
- The real AppSession import test already proves suite/relink/confirmation/direct-specular failures preserve all committed state, successful material changes advance generation exactly once, and same-size Base reload preserves independently selected PBR slots. Unicode/domain/budget failures should be added through these same product entry points.
- Existing optional external-suite validation assumes long-lived compressed bytes for all images; because the new task has no real model and S06 intentionally drops some resident snapshots, that optional expectation must be replaced by synthetic/shared-ownership invariants rather than retained as an architectural constraint.

## Errors and blockers

- S05 affected Release build repair round 1/2: `nuklear_labpbr_panel.cpp:127` has one extra closing parenthesis in the new saturated `size_t` Coverage-count display conversion. Other S05 translation units continued compiling; remove only the unmatched delimiter and rebuild the same targets.
- Planning update attempt 1 failed because one multi-hunk patch expected `## Execution decisions` after a later section. No partial edit occurred; resolved by re-reading the file and applying smaller exact-context hunks.
- One PowerShell `rg` sampler inventory used Unix-style wildcard path arguments (`src/gfx/*.cpp`), which Windows rejected with error 123. The two explicitly named Vulkan files and recursive shader search still returned useful results; subsequent inventories will use `-g` filters or directory roots.
- The first baseline-record patch used mojibake copied from PowerShell's default decoding and did not match the UTF-8 em dash in `progress.md`. No partial edit occurred; re-read with explicit UTF-8 and patched the exact text.
- The first explicit S00 staging command reported that `docs/plans/` is ignored, so none of the three required planning files were staged. Production/test paths staged correctly; the planning paths are intentionally force-added by exact filename, without broad staging or touching other ignored evidence.
- The first S01 findings append targeted only the second sentence of a combined S00 bullet and did not match. No partial edit occurred; the UTF-8 tail was re-read and the append was anchored to the complete bullet.
- The execution turn was interrupted after the S01 pure module and mesh delegation edits but before tests. Session catch-up reported three unsynchronized recovery messages; Git showed only the intended S01 candidate plus the protected pre-existing baseline, with no staged files or running build.
- A second generation inventory repeated a Windows-incompatible `src/gfx/*.cpp` positional wildcard and emitted error 123; the explicitly named AppSession/main/header searches still completed. Recursive searches will continue to use directory roots with `-g` rather than positional wildcards.
- One broad S02 call-site/field inventory exceeded the tool context limit and returned only a truncation notice. It was read-only and made no repository changes; subsequent inspection is split into bounded exact-function and exact-call-site reads.
- The first combined S02 consumer patch did not apply because the alpha-span hunk expected whitespace that differs from the header's current formatting. `apply_patch` made no partial changes; the edit is being split into exact per-file patches.
- A repetitive AppSession call-site patch matched the first generic Coverage declaration twice, producing one duplicate `domain` declaration in `refreshLabPbrAuthoring` and leaving the apply path without its declaration. The post-edit inventory caught this before compilation; it will be corrected with exact function context.
- S02 targeted Release build attempt 1 failed in `static_model_draw_plan.hpp`: after promoting alpha span bounds to `double`, four `std::min/max` calls mixed the double accumulator with float vertex members and MSVC could not deduce one template type. This is repair round 1/2; the evidence-based fix is an explicit double conversion at those four accumulation points.
- The repair-1 rebuild successfully produced both viewport and AppSession regression executables. Its final command segment used a nonexistent guessed app target `XpbdBaker`, so MSBuild returned MSB1009 after the two requested test targets were already green; this is a command-name error, not a second code-repair round. The application target will be read from CMake before rerun.
- A test-fixture syntax search included a nonexistent top-level `tests` directory, so `rg` emitted `os error 2` while still returning repository/artifact matches. No files changed; subsequent searches use only known roots.
- The first S02 CTest command used executable-style names in `-R`, but the registered CTest names use different labels, so CTest reported `No tests were found`. This is neither a pass nor a code failure; exact registered names will be read with `ctest -N` and rerun.
- An S03 picker call-site search repeated the known Windows-incompatible positional wildcard `src/app/nuklear_*.cpp` and returned error 123. It was read-only; all subsequent picker searches use the `src/app` directory with an `rg -g 'nuklear_*.cpp'` filter.
- The first combined S03 snapshot/decode patch did not apply because its final hunk expected the empty-buffer check after the source initializer, while the function stores that check before it. `apply_patch` made no partial changes; the implementation is split into exact header, helper, decoder, and path-loader patches.
- S03 targeted build repair round 1/2: the new source-contract test named both its `filesystem::path` and loaded `std::string` `texture_source`, producing one redefinition and the follow-on `path::find` errors. Production sources compiled; rename the path variable to `texture_source_path` and rebuild.
- S03 targeted test repair round 2/2: the 33-byte PNG truncation was rejected during `stbi_info_from_memory`, so both assertions expecting the later Decode stage failed while every rollback oracle remained green. Replace that fixture with a complete encoded PNG whose IDAT zlib header alone is corrupted; IHDR/Header remains valid and stb must then fail during Decode. If the repaired affected test is not green, restore S03 to `80970b6` and mark BLOCKED as required.
- Repair round 2 succeeded: corrupting only the complete PNG's first IDAT payload byte preserves a valid Header and deterministically reaches the Decode-stage failure. The AppSession regression is green 1/1 (0.12 s), including every long-path success, budget rejection, corrupt/missing/Domain rollback, and source contract.
- After repair round 2, the separately affected `xpbd_viewport_regression` exposed one remaining failure: `corrupt _s is rejected`. All preceding decoder, UV, material, strict-import, and source-contract assertions were green. The phase repair budget is exhausted, so no third assertion/diagnostic adjustment is permitted; S03 production/test changes must be restored exactly to green commit `80970b6`, Gate A must not run, and the task stops BLOCKED.
- Exact final mismatch: `viewport_regression_tests_main.cpp:1749` requires the legacy lowercase substring `decode failed`, while S03 deliberately emitted the stage-labelled `Decode stage failed`. The import itself returned `Failed` and every earlier strict-suite check passed. Updating this legacy assertion to the stronger stage contract would be a third local repair, so it was not attempted.
- Exact rollback verification: all six S03 code/test paths match `80970b6` byte-for-Git after refresh; the apparent `.M` entries immediately after `git restore` were stale index-stat records with empty raw/numstat diffs and disappeared after `git update-index --refresh`. Protected user paths were never staged, restored, deleted, or otherwise changed.
- The user explicitly authorized execution after the BLOCKED report. Treat this as a fresh, user-authorized S03 resume from exact green code `80970b6`; preserve the historical two-round failure record, reapply only the previously validated S03 scope, and update the legacy corrupt-sidecar diagnostic assertion to the stronger stage-labelled contract without weakening rejection or rollback semantics.
- S03 resumed verification first invoked `cmake --build build --config Release ...`, but repository-local `build/` has no `CMakeCache.txt`; exit 1 is a build-directory selection error, not a compiler/test failure, and consumes no repair round. Locate and reuse the actual baseline preset build tree.
- The first attempt to log that orchestration error targeted a nonexistent `## Error log` heading. `apply_patch` made no partial changes; the exact `## Errors and blockers` section was re-read and used here.
- S03 resumed targeted build repair round 1/2: `texture_image.cpp:563` passed a dynamically composed `std::string` to `setErrorNoThrow`, whose deliberate no-throw overload accepts only `const char*`. All earlier S03 translation units reached compilation. Replace only that filesystem-exception assignment with a guarded direct string assignment and a literal fallback; do not alter loader behavior.
- S03 resumed targeted test repair round 2/2: affected CTest was viewport PASS and AppSession failed only `missing Unicode Sidecar preserves the complete material session`. The fixture incorrectly named its selected Base `缺少_s.png`, so strict selection correctly rejected it as a sidecar before reaching missing-Specular discovery. Rename only that Base fixture to a non-`_s` stem; rollback assertions and production behavior remain unchanged. If the rebuilt affected tests are not fully green, restore S03 to `80970b6`, record BLOCKED, and stop.
- S03 resumed repair round 2 succeeded: after renaming only the runtime Base fixture, the rebuilt AppSession test and the unchanged viewport test pass 2/2 in 1.34 seconds. All Unicode long-path, stage diagnostic, exact-byte, and complete-session rollback assertions are green.
- S03 post-green scope audit found one preservation issue before commit: path loading decodes into an outer local candidate to keep path assignment transactional, so `decodeTexture` no longer sees the caller's existing `out.rgba.capacity()` as `previous_output_bytes`. The existing decode budget contract counted that resident allocation. Add it, with checked arithmetic, to `retained_resident_bytes` before decoding the local candidate; this is a pre-commit semantic-preservation audit correction, not a response to another failed build/test. Any subsequent red verification still requires rollback because the two failure-repair rounds are exhausted.
- The budget-preservation audit correction is green: all three affected Release targets build, and viewport/AppSession CTest passes 2/2 in 1.45 seconds. The new regression observes the exact 101-byte peak and confirms a 100-byte limit preserves the existing output.
- S03 final audit is green: all file-based Base/Suite/Iris decode paths now share bounded immutable snapshots and `_from_memory` stb calls; Windows pickers use filesystem COM dialogs without fixed buffers; stage diagnostics and complete-session rollback are covered at >330-character Chinese paths. No forbidden subsystem file is in the task diff, and no large image asset was added.
- Gate A Release build is green for app, CLI, all regression executables, SPIR-V stale detection, and the 27-shader source/binary/embed/manifest contract. Full CTest is green 5/5 in 4.30 seconds; the viewport suite supplies the CPU reference and shader/source contract evidence required independently of RT hardware.
- The first Gate A `Get-CimInstance` hardware probe exceeded its initial yield and the wrapper projected only empty output, losing the resumable session identifier. It was read-only and made no repository change; use bounded PnP/NVIDIA CLI probes instead.
- The bounded PnP probe reports a started `NVIDIA GeForce RTX 4090 Laptop GPU` plus the virtual display and a disconnected Intel adapter. Its first `nvidia-smi` invocation indexed a scalar path as though it were an array and attempted to execute `C`; this command-shape error does not affect Gate A. Retry the known executable path directly before classifying hardware verification.
- The first direct retry guessed the legacy Program Files NVSMI location, which is absent on this driver installation. The earlier existence filter therefore found the System32 copy. Probe both candidates explicitly and invoke the first verified full path without scalar indexing.
- The corrected probe succeeds: `C:\Windows\System32\nvidia-smi.exe` reports `NVIDIA GeForce RTX 4090 Laptop GPU`, driver `596.36`, compute capability `8.9`. Device presence is not itself a hardware RR/RT pass; only a repository-provided automated hardware exercise may justify that label. Gate A's explicit mandatory list is otherwise fully satisfied by the green Release build/full CTest and source/CPU regressions.
- Repository test/CMake/README inventory finds no registered RT/RR hardware test; the only hardware-smoke references are protected historical handoff/artifact records from earlier work. Do not relabel those old runs as validation of this S03 commit. Gate A can close on its explicit mandatory Release/full-CTest/source-contract criteria while current-task hardware RR/RT remains unclaimed unless a safe existing harness is found.
- Gate A is complete at S03 commit `a9b955d`: full Release and all five registered tests are green, including runtime-generated large-UV/Unicode transactions and Raster/RT/PT shader source contracts. The installed RTX 4090 is recorded as available, but no current automated RR/RT hardware harness belongs to this compact gate; hardware RR/RT is therefore explicitly not claimed rather than inferred from prior artifacts.
- The first S04 inventory repeated a Windows-incompatible positional wildcard for `src/test_support/labpbr_synthetic_fixture.*`; `rg` returned error 123 after all explicitly named material/memory/test searches had produced their results. It was read-only. Subsequent searches use exact filenames or directory `-g` filters.
- `ResolvedMaterialTable::texels` is redundant with the resident Base plus optional Normal/Specular RGBA images. It is allocated and fully decoded in all three construction paths (`resolveLabPbrMaterial`, strict import `buildMaterial`, and `buildAuthoredResolvedMaterial`), while the only production CPU consumer is `vulkan_backend.cpp` calling `sample`; GPU/RT records use dimensions, active flags, and the image resources directly.
- On-demand sampling can clamp to one texel, gather Base/Normal/Specular bytes at the same offset, and call the unchanged `decodeLabPbrTexel`. The table must therefore retain a Base image identity/pixels as well as optional sidecars; return-by-value replaces the old reference into the removed vector.
- S00's checked `LabPbrMemoryEstimateRequest` already separates resident/candidate images, encoded snapshots, decoder peak, Coverage, and Cache, and explicitly exposes the legacy resolved stride for S04 to set to zero. It is currently test-only and does not yet enforce a product import budget.
- The compact plan intentionally specifies structural components and checked pre-allocation rejection but no separate LabPBR byte-limit number. Existing texture decode uses a 1 GiB peak ceiling; S04 must avoid inventing a contradictory process-memory threshold and should expose an explicit preflight budget/request API so product entry points can use the established ceiling while tests inject smaller limits deterministically.
- The sole production `sample` caller is inside forbidden `vulkan_backend.cpp`, which this phase may not edit. Preserve the existing `const ResolvedMaterialTexel&` API with one per-thread scratch texel decoded on demand; this removes all per-pixel residency, stays thread-safe across callers, and requires no forbidden backend modification.
- `buildSessionLabPbrMaterial` is the correct pre-Coverage/Composition transaction boundary. A checked preflight context can include old public Session residency, candidate image count, encoded suite snapshots, one decoder image, full current Coverage/Composition estimates, and cache bytes, with a default 1 GiB limit plus a test-injectable Session limit. It must run before new Coverage/Composition allocation and leave every output local untouched on failure.
- The first mechanical patch to append the Session budget at all ten material-preflight calls did not apply because the Iris call's wrapping differs from the expected context. `apply_patch` made no partial changes. Re-read exact call endings and patch them in smaller groups.

## Remaining risks

- No real user model is available; correctness evidence must come from runtime-generated fixtures.
- NVIDIA RT/RR hardware availability has not yet been probed.
- The task's untouched baseline is green at local HEAD `3bd2109`: `cmake --preset s00-gate`, complete `s00-gate-release` build (app, CLI, all regression targets, and 27-shader SPIR-V verification), and CTest 5/5 passed in 4.27 seconds on 2026-08-02.

## Current implementation facts

- `GeometryDescription` has one combined `has_texture_size` flag and default 16×16 integers. The loader truncates numeric declarations to `int`, clamps them to at least 1, and sets the shared flag when either axis exists. A one-axis declaration therefore silently treats the other default as declared, while fractional/negative/too-large values are not strictly rejected.
- Cube/face authored UV values are already parsed as finite doubles. Per-Face supports negative `uv_size`, 90-degree rotations, and four-number corner arrays; Box UV supports mirror. S01 can centralize interpretation without changing loader-side LabPBR/PBR semantics.
- `StaticModelVertex` currently stores only normalized float `u/v`; `StaticModelFace` stores no raw UV bounds or domain identity. `viewport_mesh.cpp` also begins UV work with a private float `FaceTexelUV`, confirming precision and domain information are discarded before consumer stages.
- Static and dynamic meshes do call the same private `resolveCubeFaceUV`, but it converts authored doubles/default cube extents to float immediately. Both then choose one divisor from the combined declaration flag; no UV-bounds scan or recoverable atlas domain exists.
- Dynamic preview subdivides each textured face into 8×8 cells and classifies them through generic `TextureImage::sample`; the static mesh stores normalized UVs and later alpha classification samples independently. These are the two consumer paths that S01/S02 must keep aligned.
- `viewport_mesh.cpp` has been read completely. All rest/animation/baked paths funnel through the same builder, so a builder-owned resolved domain can cover static and dynamic generation without touching XPBD/Bullet or animation semantics.
- `TextureImage` exposes only the generic `sample(float,float)` API today. S02 needs an explicit model-atlas clamp sampler rather than changing this global API's established Repeat behavior.
- Generic `TextureImage::sample` explicitly wraps with fractional Repeat before nearest texel selection. File loading passes `path.string()` to `stbi_info/stbi_load`, so Windows Unicode/long-path failures occur before the otherwise-transactional candidate swap; S03 should route filesystem bytes through the existing memory decoder.
- `ResolvedMaterialTable` currently owns `vector<ResolvedMaterialTexel> texels` plus by-value Normal/Specular `TextureImage`s. Its removal and image sharing are distinct later phases as the plan requires.
- `resolveLabPbrMaterial` loads compatible sibling maps and then decodes every pixel into the persistent texel vector. `valid()`, `sample()`, and resource equality all depend on/deep-compare that expansion, and `sample()` repeats UVs. S04 must replace all four behaviors together while retaining `decodeLabPbrTexel()` unchanged.
- Legacy best-effort sidecar discovery uses filesystem-aware names but still reaches the path-string stb loader; the strict suite importer must become the canonical Unicode-safe snapshot/decode path without changing LabPBR 1.3 channel math.
- Strict suite import already snapshots with `filesystem::path` + bounded `ifstream`, validates size/time before and after the read, hashes the shared byte vector, and decodes from memory. Its material builder nevertheless copies decoded images and performs the same full texel expansion; errors use narrow `.string()` only for diagnostics/metadata.
- `LabPbrSuiteImportCache` is currently an unbounded ordered map whose `find` and `store` return/store `ImportedLabPbrSuite` by value, causing deep image/material copies even though encoded snapshots are shared.
- Cache lookup happens only after snapshotting and decoding all candidate images, so a hit avoids material construction but not decoder allocations. S07 needs shared cache entries, LRU byte accounting, and an earlier safe lookup point based on snapshot identities.
- Strict discovery/import still converts filenames/stems/extensions to narrow `.string()` for suffix logic and error text. Unicode-safe exact path construction should use native `filesystem::path` components; ASCII folding may remain only for ASCII extensions/suffixes.
- Authoring currently represents Coverage as `map<string, vector<uint32_t>>`, Composition as an always-owned `TextureImage`, and Iris normal as a second owned encoded vector plus a by-value decoded image. These match the planned S05/S06 conversion targets.
- Coverage construction currently uses a per-group `std::set<uint32_t>` and derives pixel coordinates by multiplying already-normalized float mesh UVs by atlas width/height. It clamps triangle bounds to the atlas, which can mask true out-of-domain UVs rather than reject the candidate.
- Composition allocates/fills a complete default Specular before checking whether overrides exist and deep-copies imported pixels. `buildAuthoredResolvedMaterial` copies the source/images and re-decodes every texel. These are the exact S04/S05 peak and residency sources.
- Iris import already decodes from an `ifstream` byte buffer and commits transactionally, but the read is unbounded until EOF and the byte vector is independently owned. S03 adds the same bounded snapshot discipline; S06 shares the strict suite snapshot and decoded image.
- LabPBR export stages and validates all outputs, backs up existing targets, installs, and rolls back on exception. S03's Unicode-safe global file decode also protects staged validation; later ownership changes must preserve this transaction protocol. Export stem manipulation currently narrows paths and should be made native-path-safe where touched.
- `static_model_draw_plan.hpp` deliberately computes wrapped alpha texel spans; a span of at least one normalized cycle scans the whole atlas and smaller cross-cycle spans split into two. This is the S02 behavior to replace with clamped, resolved-domain spans.
- RT primitive records copy the static mesh's normalized float UVs verbatim. Correcting the mesh/domain once will align Raster and RT records without modifying BLAS/TLAS topology or RR/FG ABI.
- `rt_scene_records.cpp` was read completely. Invalid record construction clears every candidate vector before return, so S02 needs no RT scene ownership changes; it only supplies already-correct UV/material metadata.
- AppSession currently owns Base, resolved material, Coverage, Composition, Iris asset, suite source, source material, and cache as separate value fields. Material generation is the single texture/material generation exposed to renderers, and its existing transaction helper is the correct commit boundary.
- `native_dialog.cpp` only manages prepare/finish lifecycle hooks; Win32 picker implementations and public declarations are in AppSession. S03 can replace picker internals without changing overall UI layout or dialog lifetime hooks.
- `buildSessionLabPbrMaterial` builds a candidate mesh, Coverage, Composition, and resolved material in locals and moves them only after every step succeeds, preserving the session transaction boundary. It currently does all Coverage/Composition work even with no overrides; S05 can make this helper lazy while retaining its commit order.
- `loadModel` currently commits geometry/model UI state before calling `refreshLabPbrAuthoring`; a later UV Domain/Coverage failure only appends a warning. S01/S02 must validate a candidate model against the active texture/material before replacing the current Session.
- Strict suite import builds candidate material/coverage/composition first, then commits all fields and generation. It nevertheless deep-copies suite Normal bytes into Iris and copies decoded images into multiple fields, directly motivating S06.
- `loadTexture` also constructs texture, resolved-material, Coverage, Composition, and authoring candidates before committing them, so decode/material/coverage errors already preserve its prior Session and generation. Domain validation should be inserted into that existing candidate boundary.
- `refreshLabPbrAuthoring` commits only after its local candidate build succeeds, but `loadModel` invokes it after replacing geometry and related UI/model state. A Domain/Coverage failure during model import therefore cannot currently roll back the model replacement and must be moved ahead of the Session commit in S01/S02.
- LabPBR draft initialization reads the first flat Coverage texel for each group. S05's compact run encoding therefore needs a deterministic first-texel accessor in addition to membership/iteration behavior.
- The newest available CTest log predates this task (`2026-08-01 21:53`) and reports the prior AppSession suite passing; it is historical evidence only, not the required untouched S00 baseline.
- Direct specular/normal import and removal also build all Coverage/Composition/material candidates before mutation. S03/S05/S06 can preserve these established commit points while changing loaders and ownership.
- The Win32 folder picker already uses `IFileDialog`, but file-open and save pickers still use `OPENFILENAMEW` with `MAX_PATH` buffers. S03 should replace the relevant file picker(s) with filesystem-returning `IFileOpenDialog`/`IFileSaveDialog` behavior while preserving `NativeDialogScope` and avoiding UI layout changes.
- The one model-atlas sampler in `vulkan_backend.cpp` is nearest, fixed nearest mip, and Repeat on U/V/W at the static albedo/normal/specular descriptor set. This is the only allowed backend address-mode edit. Other sky, atmosphere, font, and world samplers remain out of scope.
- Shader consumers use the same interpolated mesh UV for Base, Normal, and Specular with explicit `textureLod(..., 0.0)`. Correct atlas clamping at CPU reference sampling plus the model sampler therefore aligns all three GPU material slots without changing shader math or fixed LOD 0.
- The path tracer creates a generic clamp sampler first, then mutates the same create-info back to Repeat for its albedo/normal/specular material samplers. S02 must update those three material samplers as well as the forward/RT static sampler; the unrelated generic sampler stays unchanged.
- Existing test targets each compile their own graphics source list rather than link a shared graphics library. A test-only synthetic-fixture implementation can be compiled into both viewport and AppSession regression executables without changing application rendering behavior.
- Existing AppSession tests duplicate PNG encode/write helpers and use only tiny fixtures. The shared S00 generator should own runtime PNG/model-suite creation and later be reused by both the domain/material consumer tests and the real AppSession Unicode import entry-point test.
- `ResolvedMaterialTexel` is a large float-rich value and the current table is one element per atlas pixel. The S00 estimate will keep its bytes-per-pixel input explicit so S04 can prove the final estimate uses zero legacy resolved-texel bytes after the type's persistent vector is removed.
- S00 measured `sizeof(ResolvedMaterialTexel) == 108` on the MSVC x64 Release baseline. The explicit legacy estimates were: 1K resident 125,829,120 / peak 171,966,464 bytes; 2K resident 503,316,480 / peak 687,865,856 bytes; 4K resident 2,013,265,920 / peak 2,751,463,424 bytes.
- The checked estimator preserves its output on multiplication/addition overflow and keeps Coverage and Cache bytes separately auditable. The synthetic generator writes all PNGs only beneath a runtime temporary directory and includes large-eye, high-resolution protection, true out-of-bounds, Box, mirror, per-face rotation, negative size, Up/Down, 16,384-boundary, and non-finite cases.
- Only the loader, two mesh divisor sites, two AppSession status/warning sites, and a few tests use the combined `has_texture_size`. S01 can replace it cleanly with independent width/height declaration flags plus a complete-declaration query; a one-axis declaration remains recorded but is not treated as a reliable two-dimensional domain.
- The pure Domain decision preserves its candidate output on failure: reliable complete declaration plus fitting raw bounds selects Declared; overflow beyond the declaration but within the imported atlas selects Recovered; incomplete or absent declaration selects ImportedTexture; bounds outside every eligible domain are rejected.
- The existing private face resolver contains the authoritative Bedrock behaviors (Box layout, East/West mirror exchange, negative sizes, quarter-turn rotation, and reversed Up/Down serialized corners). S01 will move that logic to a reusable double-precision module and make the mesh consume it, rather than introduce a second interpretation.
- S01's new `uv_domain` module is independent of Vulkan and AppSession. It exposes transactional face resolution, rotated double corners, geometry bounds scanning, and Domain selection; `viewport_mesh.cpp` now delegates to that single resolver and retains doubles until its CPU-sample or GPU-vertex float boundary.
- Bedrock texture declarations now have independent presence flags. Present values are accepted only when finite, positive, integral, and at most 16,384; incomplete declarations remain observable but are deliberately not reliable domains.
- The S01 table test now exercises shared-fixture bounds and exact corners, Declared/Recovered/ImportedTexture selection, one-axis fallback, true out-of-domain and non-finite output preservation, plus loader rejection of zero, negative, fractional, oversized, string, and null declarations.
- The first S01 Release compilation and direct viewport regression passed without a repair round. Existing canonical Box/mirror/rotation/Up behavior remains green after delegating the mesh to the double resolver.
- S01 scope review contains only loader declaration data/parsing, the pure UV module, mesh delegation/precision, declaration-aware AppSession messages, shared fixture/tests, CMake, and planning files. No Vulkan, shader, PBR math, RT topology, physics, UI layout, or protected user evidence is part of the phase candidate.
- The real render loop supplies the same current `model_texture` to the static builder, and its CPU cache key already includes both model and texture generations. A mesh-owned `ResolvedUvDomain` can therefore feed Raster/RT records, Alpha, and Coverage without a new history protocol.
- `buildSessionLabPbrMaterial` is the product candidate boundary that constructs the textured static mesh before Coverage/Composition. It currently lets mesh exceptions escape and `loadModel` invokes it only after committing the new geometry; S02 must return staged Domain/coverage errors and preflight a candidate geometry/bone mapper before any Session mutation.
- Coverage multiplies normalized mesh vertices by the actual atlas extent. Once the mesh normalizes through its resolved Domain, this correctly yields raw texels for Recovered domains and scaled texels for Declared high-resolution protection, provided Coverage verifies the mesh Domain and atlas dimensions instead of accepting arbitrary floats.
- Static Alpha classification is the last CPU consumer with deliberate Repeat spans. It can use clamped texel spans over the already Domain-normalized vertex range, while a dedicated `TextureImage::sampleModelAtlasClamp` serves dynamic CPU preview without changing the generic Repeat sampler.
- `textureGeneration()` and `materialGeneration()` are the same counter. It already keys the CPU mesh cache, GPU static material upload, and path-trace history inputs, so a successful Domain/material transaction needs only the existing single generation advance; no new RR/SR/FG signal is required.
- Recovery located the current task CTest log at `out/build/s00-gate/Testing/Temporary/LastTest.log` (2026-08-02 11:12:53); the previously probed `build/s00-gate` path was simply the wrong build-root spelling.
- `buildSessionLabPbrMaterial` currently has a single local-candidate commit for Coverage/Composition/resolved material, but neither returns the mesh Domain nor converts builder exceptions into its `false`/error transaction contract. S02 will extend this helper rather than add a second material path.
- `loadModel` mutates geometry, mapper, selection/override/UI state and several generations before its post-commit refresh. S02 therefore needs a candidate mapper plus candidate material/Domain preflight before the first Session assignment; successful loads keep the current reset semantics, failed loads keep every prior field/generation.
- `clearTexture` clears every LabPBR state field together and advances the existing material generation once. The new owned Domain must be cleared in that same block and committed by every existing material transaction helper call.
- `StaticIndexedModelMesh::clear` is the natural reset point for a mesh-owned `ResolvedUvDomain`. Its vertices currently expose only float normalized `u/v`; S02 will retain double raw atlas coordinates alongside those GPU-facing floats so Coverage does not reconstruct source texels from rounded values.
- The static builder currently chooses declared dimensions unless the declaration is incomplete, so a complete-but-too-small 16×16 declaration still normalizes large-eye UVs incorrectly. It can resolve one geometry-wide Domain at build entry whenever a valid imported Base exists, store it on the mesh, and use that extent for every face/tangent/consumer.
- Dynamic preview samples 8×8 face cells through the generic Repeat sampler after interpolating double corners into float normalized coordinates. S02 will pass the same resolved extent into this path and use a dedicated model-atlas clamp sampler without changing the generic Repeat API.
- `ResolvedUvDomain` already records imported extents, selected normalization extents, declaration reliability/kind, and raw bounds. Its normalization helpers are the single source for static/dynamic UV conversion; S02 does not need a parallel domain type.
- Coverage currently has no error channel, skips malformed face/index ranges, reconstructs texels from float normalized UVs, and clamps triangle bounds. A transactional checked overload must validate the mesh Domain/imported extent and structural ranges before publishing its output; the existing value-returning wrapper may remain only for compatible test callers.
- Static alpha uses a two-period Repeat span helper, while `ResolvedMaterialTable::sample` also wraps. Both model-material consumers must clamp to `[0,1]`; generic `TextureImage::sample` remains Repeat and gains a separately named clamp method.
- The dynamic builder resolves its extent once immediately before iterating bones, so it can perform the same one-shot `resolveGeometryUvDomain` validation as the static builder and fail before emitting any model triangles; `buildFromPoses` already clears its output first.
- Vulkan scope is exact: only the three static material sampler address modes at `vulkan_backend.cpp:9792-9794` change, preserving nearest and LOD 0. The path tracer already initializes the shared create-info to Clamp, then explicitly changes only its three material samplers to Repeat; deleting those three mutations retains Clamp without touching unrelated samplers.
- Coverage's current return-by-value implementation cannot distinguish an empty valid atlas from malformed mesh/domain input. The new checked overload will preserve caller output on failure and the compatibility wrapper will return an empty object only for legacy callers/tests.
- `buildFromPoses` clears output, then copies ground geometry before its current UV extent setup. To keep Domain failure from leaving partial preview output, the one-shot textured Domain resolution must occur immediately after `out.clear()` and before ground insertion.
- Every suite/Base/specular/normal/override transaction already stages Coverage/Composition/resolved material in locals and commits those fields together. S02 adds one candidate `ResolvedUvDomain` to the same helper signature and assignment block at each call site; no extra generation counter is introduced.
- The Base/suite import `material_changed` comparisons do not need Domain equality as an additional reset condition: any imported extent/resource change is already captured by texture/material equality, while model-only Domain changes flow through the existing model generation/reset path.
- `BoneMapper` is ordinary value state and `replaceModelBones` clears only model-derived mappings before refreshing the new bones. `loadModel` can therefore stage `auto loaded_mapper = bone_mapper; loaded_mapper.replaceModelBones(...)`, preserving global mapper configuration while keeping the current Session mapper untouched until Domain/material preflight succeeds.
- The AppSession regression material snapshot currently omits the newly owned Domain and has no direct `loadModel` transaction test. S02 must add Domain equality to this oracle and explicit valid-Recovered plus truly-out-of-domain model-load rollback coverage through the real Session entry points.
- The exact application CMake target is `xpbd_baker_app`.
- Three existing Coverage tests use a manually assembled 2×2 overlapping mesh. They must attach a matching synthetic `ResolvedUvDomain` and raw vertex texels so the stricter checked Coverage path tests real invariants instead of relying on reconstructed normalized floats; an empty valid mesh likewise needs a valid imported Domain.
- `makeOverlappingLabPbrMesh` spans normalized `[0,1]` over a 2×2 atlas, so its exact raw reference coordinates are simply `u*2,v*2`; setting a 2×2 ImportedTexture Domain preserves every existing four-texel coverage expectation.
- The shared fixture paints opaque, distinct Base/Normal/Specular sentinels for body and both eye cells plus a transparent Repeat trap, enabling one runtime test to prove recovered static UVs, dynamic preview visibility, Coverage membership, and channel-aligned CPU material sampling without committed assets.
- The existing `testPathTracePbrSourceContracts` already reads both Vulkan sampler implementations and asserts nearest/fixed-LOD material behavior. S02 will add exact Clamp assertions there while retaining every existing filter/LOD assertion.
- The new material-consumer regression belongs immediately after the pure S01 Domain table test, keeping Domain selection and downstream use adjacent while leaving later unrelated preview-scene tests untouched.
- `buildAuthoredResolvedMaterial` accepts an empty source table, then decodes aligned Base/Normal/Specular bytes by one shared linear texel index. The 256×256 fixture is small enough for an S02 CPU reference check and does not invoke the 2K memory-stress policy.
- The normal AppSession CTest ends its existing suite-import regression by clearing all texture/material state and deleting its temporary directory. A following isolated Domain transaction test can therefore use the singleton safely, generate its own Unicode-named suite/models, and clean up without contaminating later tests.
- No committed unit-test JSON covers per-face `uv_size`; the runtime AppSession test will write the small Bedrock JSON directly from the already-validated loader schema (`uv` object with a `north` face containing `uv` and `uv_size`).
- Loader geometry structs do not define value equality. Model-load rollback evidence will compare the committed path/identifier/bone names/raw face UV plus model, physics, material, appearance, visibility, scene-selection and PT-reset observables, while the existing material snapshot covers GPU-facing resources and Domain.
- `path_trace_settings.reset_generation` is public Session state and is the direct PT-history rollback oracle. The runtime model JSON was re-read after insertion and is valid unescaped JSON inside raw string literals with Unicode filesystem names.
- `StaticModelVertex` is never uploaded by raw struct size/layout. Vulkan explicitly copies its fields into dedicated GPU arrays/vertices and RT records copy normalized `u/v` separately, so adding CPU-only double raw UV fields cannot alter the GPU vertex ABI.
- After repair round 1, the exact Release targets `xpbd_viewport_regression_tests`, `xpbd_app_session_regression_tests`, and `xpbd_baker_app` all build successfully; only pre-existing third-party warnings remain.
- Registered affected CTest names are `xpbd_viewport_regression` and `xpbd_app_session_regression`. Both pass after S02 (2/2, 1.33 s), including runtime large-eye/high-resolution/OOB fixtures, Clamp source contracts, channel alignment, Unicode paths, smaller-atlas rollback, and bad-model generation rollback.
- Scope audit confirms `vulkan_backend.cpp` changes exactly three model sampler address modes and `vulkan_path_tracer.cpp` only stops overriding the same three material samplers back to Repeat. Nearest filters, mip mode, fixed LOD 0, staging, queues, submits, fences, and resource ownership are untouched.
- AppSession diff audit confirms all material entry points stage and commit the owned Domain beside Coverage/Composition/resolved material. `loadModel` performs geometry/mapper/material preflight before its first Session assignment, and successful model loads retain the existing physics/model/appearance/visibility/scene/PT reset sequence.
- Mesh/consumer diff audit confirms static and dynamic builders resolve exactly once per build; raw UVs remain double while only normalized GPU UVs become float. Generic texture Repeat remains unchanged, and Alpha/material samplers now clamp without changing threshold or LabPBR decode math.
- Coverage now uses raw double UVs plus the mesh-owned Domain, verifies imported dimensions before allocation, rejects invalid face/index/domain coordinates, and publishes only its fully built local candidate. Declared high-resolution domains intentionally scale their texel coordinates across the imported atlas; Recovered domains preserve raw atlas positions.
- Re-reading the original S02 section confirms all seven required items are represented: one Static/Dynamic Domain, Domain-aware Alpha/Coverage, double raw UV, nearest/LOD0/Clamp material samplers, dedicated CPU Clamp, no cross-period Alpha scan, and existing material/model generation invalidation. Renderer code consumes the same material generation for CPU mesh refresh, GPU material updates, RT/RR generations, and PT scene history; no new protocol is needed.
- S02 final verification is green after one bounded repair: exact Release app/viewport/AppSession targets pass, affected CTest passes 2/2 in 1.30 s, and `git diff --check` passes. No 2K stress was run (correctly reserved for memory-structure phases), and no Vulkan staging/submit/fence/queue/resource-ownership code changed.

## S03 Unicode / long-path facts

- The original S03 gate requires all Base, strict Suite, and Iris Normal imports to follow `filesystem::path -> bounded ifstream snapshot -> stbi_info_from_memory -> stbi_load_from_memory`, with stage-specific file-check/read/header/decode/domain/sidecar/budget errors and rollback for Unicode, corrupt, or missing inputs.
- AppSession still has one `GetOpenFileNameW` and two `GetSaveFileNameW` implementations backed by fixed `MAX_PATH` arrays. The folder picker already uses `IFileDialog`; S03 should reuse that COM lifecycle style for open/save without changing UI layout.
- Generic `loadTextureImage(path)` is the Base and direct-specular path and still reaches stb through a narrowed path. Strict Suite already snapshots through a bounded `ifstream`; Iris Normal uses an unbounded stream-to-vector read and must adopt the common bounded snapshot/decode path.
- `texture_image.cpp` has one decode core whose `TextureDecodeSource` still branches between filename-based stb and memory-based stb. S03 can delete the filename branch entirely: path loading snapshots bytes first, then calls the same `stbi_info_from_memory`/`stbi_load_from_memory` budgeted transaction used by in-memory callers.
- Strict Suite's existing snapshot correctly checks regular-file/size/time before and after an exact read and stores immutable shared bytes, but the implementation is private and duplicates the desired Base/Iris path. A shared transactional byte-snapshot module can preserve those invariants while giving every caller consistent file-check/read/budget stage errors.
- Iris Normal currently reads with unbounded iterators, then decodes transactionally. It must use the shared size-bounded snapshot, preserve exact encoded bytes, and commit only after RGBA/dimension checks; S06 will later change that vector ownership to shared immutable storage.
- The plan specifies bounded snapshots but no new encoded-byte constant. Preserve the current Suite-compatible upper bound of `INT_MAX` bytes, enforce it before allocation/read, catch allocation failures, and let the existing decoded/peak budgets reject oversized decoded images; S04 later adds final-model memory budgeting.
- All Base/Suite/Iris targets already compile `texture_image.cpp`. Exposing a small immutable `FileByteSnapshot` and `snapshotFileBytes` API from `texture_image.hpp/.cpp` avoids a new duplicated CMake source list while still giving every import path one implementation.

### S03 inventory continuation

- Existing regression coverage only requires the substring `stbi_info` for a damaged in-memory image, so stage-specific diagnostics can add `Header` while retaining that contract.
- Strict-suite discovery and sidecar construction currently narrow Unicode stems through `.string()`; construct sibling paths from native `std::filesystem::path` components instead.
- Suite comparison diagnostics and decoded-path metadata also use `.string()`; route them through a UTF-8 display helper so Chinese and other Unicode filenames survive diagnostics and cache metadata.
- Context-recovery inspection confirms `HEAD` is still the green S02 commit `80970b6`; no S03 production/test edits or staged changes existed, and the protected user-owned dirty paths remain unchanged.
- The original S03 text confirms four exact deliverables: `IFileOpenDialog` without fixed `MAX_PATH`; one bounded filesystem-byte-memory decode chain for Base/Suite/Iris; stage-labelled errors; and rollback for Chinese, corrupt, and missing paths.
- The recovered `LastTest.log` ends with the S02 AppSession Unicode/large-UV transaction cases passing and `xpbd_app_session_regression` green at 2026-08-02 11:53 CST, matching the recorded 2/2 affected-test baseline.
- AppSession contains exactly three fixed-buffer file pickers: one open path plus JSON and PNG save paths. Converting the shared open and both save helpers to `IFileOpenDialog`/`IFileSaveDialog` removes the fixed picker boundary without touching the unrelated executable-location helper in `i18n.cpp` or changing UI layout.
- Native sidecar construction can append `_s`/`_n` directly to `filesystem::path::stem()` and then append the original extension; case-insensitive directory fallback should compare native path strings with ASCII-only folding, avoiding a lossy Unicode stem round-trip.
- `TextureDecodeSource` is a thin filename-or-memory branch; deleting its filename member lets every decode call the `_from_memory` APIs while leaving all existing dimension, decoded-byte, peak, exception, and transactional output checks centralized in `decodeTexture`.
- The strict importer duplicates the exact snapshot invariants needed globally (regular-file check, `INT_MAX` bound, size/time checks around an exact read). Replacing only that private body with the shared snapshot API preserves its SHA/source model and avoids touching S04's material-table work.
- Strict-suite snapshot call sites identify Base, mandatory Specular Sidecar, optional Normal Sidecar, and properties Sidecar separately; carrying that label through the shared snapshot helper yields the required stage-specific diagnostics without changing suite transaction order.
- Iris currently commits only after decode/channel/dimension validation but reads with unbounded iterators and narrows its decoded path. It can retain the same by-value byte ownership until S06 by copying the common immutable snapshot only into the fully validated local candidate.
- The picker helpers are public AppSession utilities used by model, animation, Base, LabPBR, world, render-settings, and export panels. Their existing packed Win32 filter parameter must be translated centrally to COM filter specs so all current call sites and UI layout remain unchanged.
- `openFolderDialog` already establishes the required COM initialization, owner-window, filesystem-item extraction, and `NativeDialogScope` pattern; the file helpers can share equivalent RAII-safe logic while adding file-type/default-extension settings.
- Existing decode regressions assert stage-relevant substrings and complete output preservation, not an exact peak formula, so clearer `Header`/`Decode`/`budget` labels can be added without weakening any memory guard or oracle.
- AppSession already exposes a comprehensive material snapshot/equality oracle and runtime Unicode fixture setup. S03 tests can extend that product test with long nested paths plus corrupt/missing Base, Suite, and Iris attempts, proving Session/material generation rollback at real entry points.
- The plan fixes the S03 isolated commit message as `fix: support unicode and long texture paths`; Gate A follows immediately after that fourth task commit.
- All picker filters are legacy packed name/pattern pairs (one to three entries), so a bounded parser can preserve their exact visible labels/patterns before calling COM `SetFileTypes`.
- No application `longPathAware` manifest is present. To make picker-returned paths work independently of host policy, filesystem inspection/read operations should use an internal Windows extended-length I/O path while preserving the normal absolute path for Session metadata and UTF-8 diagnostics.
- The migrated app, viewport, and AppSession Release targets all compile after introducing the shared snapshot, native sidecars, Iris migration, and COM dialog helper; no code-repair round has been consumed so far in S03.
- The existing AppSession material oracle and test order allow a separate long-path test immediately after the ordinary suite test, followed by the large-UV test. It can leave no state behind by clearing the singleton and removing the short temporary root through the extended-length I/O spelling.
